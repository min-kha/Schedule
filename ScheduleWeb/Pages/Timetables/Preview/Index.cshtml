@page
@model ScheduleWeb.Pages.Timetables.Preview.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Classroom)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Room)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Slot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Subject)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timetable[0].Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Timetable) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Classroom.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Slot.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.Email)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
