@page
@model ScheduleWeb.Pages.Attends.IndexModel

@{
    var folderName = new System.IO.DirectoryInfo(Request.Path).Name; ViewData["Title"] = $"{folderName} List";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Attend[0].Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attend[0].Student)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Attend[0].TimeTable)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Attend) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TimeTable.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
